version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci --cache .npm --prefer-offline
    build:
      commands:
        - npm run build -- --output-path=dist/coreui-free-angular-admin-template
  artifacts:
    baseDirectory: dist/coreui-free-angular-admin-template
    files:
      - '**/*'
  cache:
    paths:
      - .npm/**/*
redirects:
  - source: '</^((?!\.).)*$/>'
    target: /index.html
    status: 200